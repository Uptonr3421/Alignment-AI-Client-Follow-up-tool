# ============================================
# Firebase Configuration
# ============================================
# These values come from Firebase Console
# Get them from: https://console.firebase.google.com
# Go to Project Settings > General > Your apps > SDK setup and configuration

# Frontend Environment Variables (Vite)
# Copy these to: src/web/frontend/.env
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=cleveland-lgbtq-center.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=cleveland-lgbtq-center
VITE_FIREBASE_STORAGE_BUCKET=cleveland-lgbtq-center.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id_here
VITE_FIREBASE_APP_ID=your_app_id_here

# API URL - automatically points to Firebase Functions in production
# For local development with emulators, use:
# VITE_API_URL=http://localhost:5001/cleveland-lgbtq-center/us-central1/api
# For production (auto-configured):
# VITE_API_URL=https://us-central1-cleveland-lgbtq-center.cloudfunctions.net/api

# ============================================
# Gmail OAuth Configuration
# ============================================
# These are set via Firebase CLI, not in .env files:
# firebase functions:config:set gmail.client_id="YOUR_CLIENT_ID"
# firebase functions:config:set gmail.client_secret="YOUR_CLIENT_SECRET"
#
# Get OAuth credentials from:
# https://console.cloud.google.com
# APIs & Services > Credentials > Create OAuth 2.0 Client ID
#
# Required settings:
# - Application type: Web application
# - Authorized redirect URIs: 
#   https://us-central1-cleveland-lgbtq-center.cloudfunctions.net/api/gmail/callback
#
# After creating credentials, run:
# firebase functions:config:set gmail.client_id="YOUR_CLIENT_ID"
# firebase functions:config:set gmail.client_secret="YOUR_CLIENT_SECRET"
# firebase deploy --only functions

# ============================================
# Optional: Development Settings
# ============================================
# Only needed for local development with emulators

# Use Firebase emulators (true/false)
# VITE_USE_EMULATORS=true

# Emulator ports (defaults shown)
# VITE_AUTH_EMULATOR_PORT=9099
# VITE_FIRESTORE_EMULATOR_PORT=8080
# VITE_FUNCTIONS_EMULATOR_PORT=5001

# ============================================
# Important Notes
# ============================================
# 
# 1. NEVER commit .env files to git
# 2. Firebase Studio will auto-populate most values during deployment
# 3. Gmail OAuth must be configured manually after deployment
# 4. All values can be found in Firebase Console
# 5. For production, use Firebase Hosting which auto-injects config
# 
# Need help? See firebase-studio-handoff.md for detailed setup instructions
